# Findings: Existing .gitignore Configuration

## Source: Root `.gitignore`

Covers:
- Java build artifacts (`target/`, `build/`)
- IDE files (`.idea/`, `.iml`, `.iws`)
- Frontend build (`src/main/frontend/node_modules/`, `.vite/`)
- Plugins node_modules: `plugins/*/node_modules/`
- LiteLLM env: `litellm/.env`

**Gaps identified:**
- `litellm/.env.local` — NOT ignored (contains OPENAI_API_KEY, ANTHROPIC_API_KEY)
- `plugins/*/.env.local` — NOT ignored
- `plugins/*/.next/` — NOT ignored (Next.js build artifacts)
- `plugins/*/baml_client/` — NOT ignored (generated BAML TypeScript code)
- `plugins/*/next-env.d.ts` — NOT ignored (generated by Next.js CLI)
- `.claude/settings.local.json` — NOT ignored (user-local Claude Code permissions)

## Source: `plugins/ai-description/.gitignore`

```
node_modules/
.next/
baml_client/
.env
```

**Gaps:**
- `.env.local` — NOT ignored (plugin's `.env.local` contains ANTHROPIC_API_KEY — currently visible to git)

## Source: `plugins/koszyk/`

**No `.gitignore` exists at all.** As a result:
- `.env.local` (ANTHROPIC_API_KEY) — untracked but would be committed if staged with `git add .`
- `.next/` (build output) — would be committed if staged with `git add .`
- `baml_client/` (generated TypeScript from BAML) — would be committed if staged with `git add .`
- `next-env.d.ts` (generated by `next dev`) — would be committed if staged with `git add .`
